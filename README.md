This repo contains the example demonstrated at Gophercon 18 Lightning talk.

## Building

To build this do the following.

    $ cd client && npm install && npm run build && cd ..
    $ go generate
    $ make [osx|linux]
    $ ./embedded_resources